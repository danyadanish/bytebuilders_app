name: khaberny
description: "A governmental app to connect citizens, advertisers, and municipalities."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase dependencies - updated for compatibility
  firebase_auth: ^5.5.3
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  cloud_firestore: ^5.6.7
  flutter_local_notifications: ^19.2.0

  # Toast messages - downgraded as suggested by error message
  fluttertoast: ^8.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/khaberny_background.png
    - assets/ads.jpg
    - assets/citizen.jpg

  fonts:
    - family: Cairo
      fonts:
        - asset: fonts/Cairo-Regular.ttf
        - asset: fonts/Cairo-Bold.ttf
          weight: 700